# Attribute (literal-valued) relations
#
# Objects of these relations are literals, not entities, so they stay out of the
# entity set — and therefore out of the path graph. facts/query.dl asks for a
# path to one of them (2031-02-01) so the golden report pins the refusal.
released_on
headcount
league_rank
valuation
market_cap
load_factor
